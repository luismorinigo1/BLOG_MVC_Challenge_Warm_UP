@model IEnumerable<BLOG.Models.posteos>

@{
    ViewBag.Title = "Editar Posteos";
}

<div class="jumbotron"> <h2>EDITAR POSTEOS</h2> </div>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contenido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Imagen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categoria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contenido)
        </td>
        <td>
            @*<img width="100" src="@Url.Action("getImage","EditarPosteos",(modelItem => item.Imagen))"*@
            @*@Html.DisplayFor(modelItem => item.Imagen)*@
            <img src="@Url.Action("getImage","EditarPosteos",@System.Convert.ToBase64String(item.Imagen)))" alt="Imagen" width="80" height="80" />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.ActionLink("EDITAR", "Edit", new { id=item.Id }) 
            
        </td>
    </tr>
}

</table>
